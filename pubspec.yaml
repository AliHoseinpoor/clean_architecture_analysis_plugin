name: clean_architecture_analysis_plugin
description: Custom analyzer plugin for enforcing Pulse architecture and coding conventions.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  analysis_server_plugin: ^0.3.20
  analyzer: ^14.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.2